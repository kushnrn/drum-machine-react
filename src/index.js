import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.js';

/**
 * The root is used to render App into the DOM.
 */
ReactDOM.createRoot(document.getElementById("root")).render(<App />);